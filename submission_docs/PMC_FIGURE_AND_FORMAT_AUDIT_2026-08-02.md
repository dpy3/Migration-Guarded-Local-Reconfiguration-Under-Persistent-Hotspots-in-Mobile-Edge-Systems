# Pervasive and Mobile Computing figure and format audit (2026-08-02)

## Target journal

The manuscript is prepared for *Pervasive and Mobile Computing* (Elsevier)
using the `elsarticle` preprint source and separate figure files.

## Figure changes in this revision

- Fig. 0 explicitly distinguishes the local-trial guard from final-repair
  overrides.
- Figs. 2--7 were regenerated from frozen revision CSV files only.
- Labels, titles, panel spacing, and legends were enlarged for single-column
  readability.
- Fig. 3 was re-laid out after visual QA to remove title--legend overlap.
- Fig. 3 now uses a dedicated title row and a bottom shared legend; the revised
  panel was regenerated from the frozen analysis CSV and checked at preview
  resolution.
- Every active figure is exported as editable-text PDF and SVG, 600-dpi TIFF,
  and PNG preview; source-data CSV files are retained in the revision bundle.
- MigrationMB is labelled consistently as a profile-derived volume proxy.
- The manuscript now states explicitly that 5% is the frozen primary operating
  point for this protocol, not a universally optimal budget.
- The declaration block now identifies author confirmation as the only
  remaining publication metadata step and does not imply that a public DOI
  already exists.

## Submission actions still controlled by authors

Replace anonymous author metadata, CRediT roles, funding, competing interests,
data/code availability URL or DOI, and any Elsevier-required highlights or
graphical abstract fields before upload.

The current main source uses Elsevier's official `elsarticle` class distributed
with the submission package. The source is a preprint-style manuscript, which
is the normal author-submission form; the journal's production layout is not
expected at initial submission.
